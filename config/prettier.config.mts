import {type Config} from "prettier";

const config: Config = {
    tabWidth: 4,
    printWidth: 200,
    trailingComma: "es5",
    bracketSpacing: false,
    arrowParens: "avoid",
};

export default config;
