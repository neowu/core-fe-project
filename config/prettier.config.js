// @ts-check

/** @type {import("prettier").Options} */
const config = {
    tabWidth: 4,
    printWidth: 200,
    trailingComma: "es5",
    bracketSpacing: false,
    arrowParens: "avoid",
};

module.exports = config;
